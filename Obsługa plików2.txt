
#include <stdio.h>
#include <stdlib.h>
#define MAX 40

int main(void)
{
    FILE *wp;
    char slowa[MAX];
    if((wp=fopen("slowka","w+"))==NULL)
    {
        fprintf(stdin,"Nie moge otworzyc pliku \"slowka\".\n");
        exit(1);
    }

    puts("Podaj slowa, ktore maja zostac dodane do pliku:");
    puts("Aby zakonczyc, wcisnij ENTER na poczatku wiersza.");

    while(gets(slowa)!=NULL && slowa[0]!='\0')
        fprintf(wp,"%s",slowa);
    puts("ZawartoϾ pliku:");
    rewind(wp);
    while(fscanf(wp,"%s",slowa)==1)
        puts(slowa);
    if(fclose(wp)!=0)
        fprintf(stderr,"Blad przy zamykaniu pliku.\n");

    return 0;
}





#include <stdio.h>
#include <stdlib.h>
#define MAX 40

int main(void)
{
    FILE *wp;
    char slowa[MAX];
    int zarobki[3];
    zarobki[0]=14;
    zarobki[1]=151;
    zarobki[2]=1762;


    if((wp=fopen("slowka2.txt","w"))==NULL)
    {
        fprintf(stdin,"Nie moge otworzyc pliku \"slowka\".\n");
        exit(1);
    }
    fwrite(zarobki,sizeof(int),10,wp);
   /* puts("Podaj zdanie:");
    fgets(slowa,MAX,stdin);
    fputs(slowa,wp);
   /* puts("Podaj slowa, ktore maja zostac dodane do pliku:");
    puts("Aby zakonczyc, wcisnij ENTER na poczatku wiersza.");

    while(gets(slowa)!=NULL && slowa[0]!='\0')
        fprintf(wp,"%s",slowa);
    puts("ZawartoϾ pliku:");
    rewind(wp);
    while(fscanf(wp,"%s",slowa)==1)
        puts(slowa);
    */


    if(fclose(wp)!=0)
        fprintf(stderr,"Blad przy zamykaniu pliku.\n");

    return 0;
}

